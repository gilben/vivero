# pj6-backend

Generated by **lv-agents** — Clean Architecture in .NET 10.

## Quick start

```bash
dotnet restore
dotnet run --project src/Pj6.API
```

## Agent

- **Cursor**: open this folder — `.cursorrules` activates automatically
- **Claude Projects**: paste `CLAUDE.md` into Project Instructions

## User stories

Add `.md` files to `docs/user-stories/` and run:

```bash
npx agent-docs
```
