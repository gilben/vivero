# Vitest carga los .env del proyecto: sin esto el mock de auth se filtra a los tests.
# El test de flujo simulado opta por el mock explícitamente con vi.stubEnv.
VITE_AUTH_MOCK=false
# El chat siempre usa el mock en tests (nunca llama APIs externas)
VITE_AI_PROVIDER=mock
VITE_AI_API_KEY=
